# Idempotent Property Of Binary Operations

## Definition

A binary operation * on a set S is idempotent if and only if a*a = a for all a ∈ S.

### Examples

- ∪ and ∩ are idempotent on the Universal set.